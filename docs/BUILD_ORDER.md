# Detour Build Order

Use vertical slices. Do not move on because files exist; move on because the phase works.

1. **Phase 0:** repo/config/Lakebase/schema/health/base page.
2. **Phase 1:** prove Llama chat + tool call, MiniLM embedding, pgvector search, Open-Meteo, Wikimedia.
3. **Phase 2:** arbitrary destination ingestion/caching + semantic attraction retrieval.
4. **Phase 3:** create trip + forecast/AQI + itinerary + resilience + dashboard.
5. **Phase 4:** Rainstorm → repair agent → pending diff → Apply Detour → persistence.
6. **Phase 5:** trace UI, loading/error states, demo prewarm, polish.
7. **Phase 6:** deploy, adversarial test, screenshots, README, package submission.

The hard checkpoint is the Boulder happy path. If Boulder works, test Miami and one other arbitrary city before polishing presets.
